<h1>Posts page</h1>

<%= link_to "New post", posts_new_path %>
<br>

<% @posts.each do |post| %>
    <p>
        [<%= post.id %>] <%= post.title %>
    </p>
    <p>
        <%= post.body %>
    </p>

    <%= link_to "Show", post %>

    <%= render 'posts/shared', post: post %>

<% end %>
